import { CheckCircle2, Clock3, MapPin } from "lucide-react";
import type { RouteResponse } from "../../types";
import { riskStyles } from "../../utils/risk";
function RouteSummary({ route }: { route: RouteResponse }) { const style = riskStyles[route.riskLevel]; return <section className="rounded-xl border border-cyan-400/20 bg-slate-900 p-5"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-cyan-400">Safest route found</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-lg bg-slate-950 p-4"><MapPin size={17} className="text-cyan-400" /><p className="mt-3 text-xs text-slate-400">Distance</p><p className="mt-1 text-xl font-bold">{route.distance}</p></div><div className="rounded-lg bg-slate-950 p-4"><Clock3 size={17} className="text-cyan-400" /><p className="mt-3 text-xs text-slate-400">Estimated time</p><p className="mt-1 text-xl font-bold">{route.estimatedTime}</p></div></div><div className="mt-4 flex items-center justify-between rounded-lg border border-slate-800 bg-slate-950 p-4"><div><p className="text-xs text-slate-400">Risk score</p><p className="mt-1 text-sm font-semibold">{route.riskScore}% exposure</p></div><span className={`rounded border px-2 py-1 text-xs font-bold ${style.badge}`}>{route.riskLevel}</span></div><p className="mt-5 flex items-center gap-2 text-sm text-slate-300"><CheckCircle2 size={16} className="text-emerald-400" />{route.avoidedSegments} dangerous road segments avoided</p></section>; }
export default RouteSummary;
