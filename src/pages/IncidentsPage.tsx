import { AlertTriangle, MapPin } from "lucide-react";
import { incidents } from "../data/mockData";
import { riskStyles, statusStyles } from "../utils/risk";
function IncidentsPage() {
  return <div className="mx-auto max-w-6xl space-y-5"><section className="flex flex-col gap-3 border-b border-slate-200 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-slate-500">Field reports</p><h1 className="mt-1 text-2xl font-semibold text-slate-900">Incidents</h1><p className="mt-1.5 text-sm leading-6 text-slate-600">Reports submitted by communities and field teams.</p></div><span className="w-fit border border-slate-200 bg-white px-3 py-2 text-sm text-slate-600">{incidents.length} recent reports</span></section><div className="grid gap-4 md:grid-cols-2">{incidents.map((incident) => <article key={incident.id} className="border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className={`flex h-9 w-9 shrink-0 items-center justify-center ${riskStyles[incident.severity].badge}`}><AlertTriangle size={17} /></div><div><h2 className="font-semibold text-slate-900">{incident.type}</h2><p className="mt-1 text-xs text-slate-500">Reported {incident.timestamp}</p></div></div><span className={`shrink-0 border px-2 py-1 text-xs font-semibold ${statusStyles[incident.status]}`}>{incident.status.charAt(0) + incident.status.slice(1).toLowerCase()}</span></div><p className="mt-4 text-sm leading-6 text-slate-700">{incident.description}</p><div className="mt-4 flex items-center justify-between gap-3 border-t border-slate-200 pt-3"><p className="flex items-center gap-1.5 text-sm text-slate-600"><MapPin size={15} className="text-slate-500" />{incident.location}</p><span className={`border px-2 py-1 text-xs font-semibold ${riskStyles[incident.severity].badge}`}>{riskStyles[incident.severity].label}</span></div></article>)}</div></div>;
}

export default IncidentsPage;
