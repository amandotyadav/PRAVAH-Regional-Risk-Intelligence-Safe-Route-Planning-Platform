import { AlertTriangle, MapPin } from "lucide-react";
import { incidents } from "../data/mockData";
import { riskStyles, statusStyles } from "../utils/risk";
function IncidentsPage() {
  return <div className="mx-auto max-w-6xl space-y-6"><section className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-cyan-400">Field operations</p><h1 className="mt-2 text-2xl font-bold">Active incidents</h1><p className="mt-2 text-sm text-slate-400">Review reports from monitored routes and communities.</p></div><span className="w-fit rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-xs text-slate-300">{incidents.length} recent reports</span></section><div className="grid gap-4 md:grid-cols-2">{incidents.map((incident) => <article key={incident.id} className="rounded-xl border border-slate-800 bg-slate-900 p-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-lg ${riskStyles[incident.severity].badge}`}><AlertTriangle size={17} /></div><div><h2 className="font-semibold">{incident.type}</h2><p className="mt-1 text-xs text-slate-500">{incident.id} · {incident.timestamp}</p></div></div><span className={`shrink-0 rounded border px-2 py-1 text-[10px] font-bold ${statusStyles[incident.status]}`}>{incident.status}</span></div><p className="mt-4 text-sm leading-6 text-slate-300">{incident.description}</p><div className="mt-4 flex items-center justify-between gap-3 border-t border-slate-800 pt-3"><p className="flex items-center gap-1.5 text-xs text-slate-400"><MapPin size={14} className="text-cyan-400" />{incident.location}</p><span className={`rounded border px-2 py-1 text-[10px] font-bold ${riskStyles[incident.severity].badge}`}>{incident.severity}</span></div></article>)}</div></div>;
}

export default IncidentsPage;
